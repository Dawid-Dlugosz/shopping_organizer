import 'package:flutter/material.dart';

class ShopngLists extends StatelessWidget {
  const ShopngLists({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Shopping list');
  }
}
